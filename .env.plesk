# ═══════════════════════════════════════════════════════════════
# Variables d'environnement - CRM sur Plesk
# Supabase self-hosted via Docker
# ═══════════════════════════════════════════════════════════════

# ── Supabase Self-Hosted ──────────────────────────────────────
# Remplacer par ton domaine ou IP locale si Supabase tourne sur le même serveur
# Le port 8000 est le Kong API Gateway de Supabase
NEXT_PUBLIC_SUPABASE_URL=http://supabase-kong:8000
NEXT_PUBLIC_SUPABASE_ANON_KEY=REMPLACER_PAR_TA_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=REMPLACER_PAR_TA_SERVICE_ROLE_KEY

# ── Base de données PostgreSQL (Supabase intègre PostgreSQL) ──
# Le hostname est le nom du service Docker de Supabase
DATABASE_URL=postgresql://postgres:REMPLACER_PAR_TON_MDP@supabase-db:5432/postgres
DIRECT_URL=postgresql://postgres:REMPLACER_PAR_TON_MDP@supabase-db:5432/postgres

# ── Cron Secret ───────────────────────────────────────────────
CRON_SECRET=REMPLACER_PAR_UN_SECRET_FORT

# ── OpenAI (optionnel) ───────────────────────────────────────
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ── Ollama local (optionnel) ─────────────────────────────────
OLLAMA_URL=http://host.docker.internal:11434
OLLAMA_MODEL=mistral
