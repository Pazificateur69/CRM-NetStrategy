# ═══════════════════════════════════════════════════════════════
# CRM NetStrategy - Configuration Docker Production
# ═══════════════════════════════════════════════════════════════
#
# 1. Copiez ce fichier:  cp .env.docker.example .env.docker
# 2. Configurez les valeurs ci-dessous
# 3. Lancez:             ./deploy.sh
#
# ═══════════════════════════════════════════════════════════════

# ─── DOMAINE ────────────────────────────────────────────────
# Votre domaine de production (sans https://)
DOMAIN=crm.monitoring.net-strategy.fr

# Port expose par Docker (Plesk fait le proxy SSL vers ce port)
NGINX_PORT=8080

# ─── BASE DE DONNEES ────────────────────────────────────────
DB_DATABASE=crm_production
DB_USERNAME=crm_user
DB_PASSWORD=CHANGEZ_MOI
DB_ROOT_PASSWORD=CHANGEZ_MOI_AUSSI

# ─── SECURITE LARAVEL ───────────────────────────────────────
# Laissez vide, sera genere automatiquement au premier lancement
APP_KEY=

# ─── IA (optionnel) ─────────────────────────────────────────
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=mistral

# ─── EMAIL (optionnel) ──────────────────────────────────────
MAIL_MAILER=log
MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM_ADDRESS=noreply@net-strategy.fr
MAIL_FROM_NAME="CRM NetStrategy"
